'use babel';

import ClangCompleteGeneratorView from '../lib/clang-complete-generator-view';

describe('ClangCompleteGeneratorView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
